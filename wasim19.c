#include<stdio.h>
int main()
{
    int length,width;
    printf("Enter the length and width\n");
    scanf("%d%d",&length,&width);
    printf("Area of rectangle is=%d",length*width);
    printf("\n");
    return 0;
}